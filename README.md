# Logisim Clone - Digital Circuit Simulator

Полнофункциональный симулятор цифровых логических схем с визуальным редактором и генерацией схем по текстовым промптам.

## Возможности

✨ **Основные функции:**
- 🎨 Визуальный редактор схем с drag-and-drop
- ⚡ Симуляция в реальном времени и пошаговая
- 🤖 Генерация схем по текстовым командам
- 🧠 **AI-генерация схем на русском языке** (через Ollama)
- 💾 Сохранение и загрузка схем
- 📦 Богатая библиотека компонентов

## Установка

### Требования
- Python 3.8 или выше
- pip (менеджер пакетов Python)

### Установка зависимостей

```bash
cd /Users/timmy/Desktop/logisim
pip install -r requirements.txt
```

## Запуск приложения

```bash
python main.py
```

## Быстрый старт

### 1. Создание схемы вручную

1. Выберите компонент из палитры слева
2. Кликните на холсте, чтобы разместить компонент
3. Повторите для других компонентов
4. Используйте кнопки симуляции внизу для тестирования

### 2. Создание схемы по промпту

Введите команду в поле внизу экрана:

```
half adder
```

Нажмите Enter или кнопку "Execute" - схема полусумматора будет создана автоматически!

## 🧠 AI-генерация схем (Новое!)

### Естественный язык на русском

С интеграцией Ollama вы можете создавать схемы, используя **свободные команды на русском языке**:

```
создай полусумматор
добавь два AND вентиля
построй SR защелку
сделай схему с тремя переключателями
# Logisim Clone - Симулятор цифровых схем

Современный симулятор цифровых схем с калькулятором булевых функций и автоматическим построением схем.

## ✨ Основные возможности

### 🧮 Калькулятор булевых функций

**Автоматический анализ и построение схем:**
- Таблица истинности для любой булевой функции
- СДНФ (Совершенная дизъюнктивная нормальная форма)
- СКНФ (Совершенная конъюнктивная нормальная форма)
- Полином Жегалкина (алгебраическая нормальная форма)
- **Автоматическое построение схемы** из результата

**Поддерживаемые операторы:**

| Оператор | Обозначения | Описание |
|----------|-------------|----------|
| AND | `&`, `*`, `∧` | Конъюнкция (И) |
| OR | `\|`, `+`, `∨` | Дизъюнкция (ИЛИ) |
| NOT | `!`, `~`, `¬` | Отрицание (НЕ) |
| XOR | `^`, `⊕` | Исключающее ИЛИ |
| IMPL | `->`, `→` | Импликация |
| EQ | `=`, `≡` | Эквивалентность |

**Примеры:**
```
A&B              → Простая конъюнкция
A|B|C            → Дизъюнкция трёх переменных
(A&B)|(~A&C)     → Сложное выражение
A^B              → XOR (сумма по модулю 2)
A->B             → Импликация
```

### 📊 Просмотр схем

- **Интерактивный канвас** с зумом и перемещением
- **Цветовая индикация** сигналов (зелёный = HIGH, серый = LOW)
- **Ортогональная маршрутизация** проводов
- **Слайдер зума** от 30% до 300%

---

## 🚀 Быстрый старт

### Установка

```bash
# Клонировать репозиторий
git clone <repository-url>
cd logisim
```

### Быстрый запуск

**Windows:**
Запустите файл `run_windows.bat` (двойным кликом).

**macOS / Linux:**
Запустите скрипт в терминале:
```bash
./run_mac_linux.sh
```

Скрипты автоматически установят необходимые библиотеки и запустят приложение.

### Ручная установка

```bash
# Установить зависимости
pip install -r requirements.txt

# Запустить приложение
python main.py
```

### Использование

1. **Откройте вкладку "Калькулятор"**
2. **Введите булеву функцию**, например: `A&B`, `A^B`, `(A|B)&C`
3. **Нажмите "Вычислить"**
4. **Просмотрите результаты:**
   - Таблица истинности
   - СДНФ, СКНФ, Полином Жегалкина
5. **Переключитесь на "Просмотр схемы"**
6. **Схема построена автоматически!**

### Управление схемой

- **Зум:** Слайдер или колесико мыши
- **Перемещение:** Зажмите левую кнопку мыши и тяните
- **Сброс вида:** Кнопка с иконкой прицела

---
### 3. Интерактивные элементы

- **Switch (Переключатель)**: Кликните для переключения состояния
- **Button (Кнопка)**: Кликните для нажатия
- **LED (Светодиод)**: Показывает состояние сигнала

## Доступные компоненты

### Логические вентили
- **AND** - Логическое И
- **OR** - Логическое ИЛИ
- **NOT** - Логическое НЕ (инвертор)
- **XOR** - Исключающее ИЛИ
- **NAND** - НЕ-И
- **NOR** - НЕ-ИЛИ
- **XNOR** - Эквивалентность
- **Buffer** - Буфер

### Ввод/Вывод
- **Switch** - Переключатель
- **Button** - Кнопка
- **LED** - Светодиод
- **InputPin** - Входной пин
- **OutputPin** - Выходной пин
- **Clock** - Генератор тактовых импульсов

### Память
- **DFlipFlop** - D-триггер
- **JKFlipFlop** - JK-триггер
- **SRLatch** - SR-защелка
- **Register** - Регистр

### Сложные компоненты
- **Multiplexer** - Мультиплексор
- **Decoder** - Декодер
- **HalfAdder** - Полусумматор
- **FullAdder** - Полный сумматор
- **Comparator** - Компаратор

## Команды промптов

### Создание готовых схем

```
half adder          # Создать полусумматор
full adder          # Создать полный сумматор
sr latch            # Создать SR-защелку
simple circuit      # Создать простую схему с AND вентилем
```

### Добавление компонентов

```
add and             # Добавить AND вентиль
add switch          # Добавить переключатель
add led             # Добавить светодиод
create not          # Создать NOT вентиль
```

### Расширенные команды

```
add and at 100 200 label MyGate    # Добавить AND в позиции (100, 200) с меткой
clear                               # Очистить схему
list                                # Показать все доступные компоненты
help                                # Показать справку
```

## Управление симуляцией

- **Start** - Запустить непрерывную симуляцию
- **Stop** - Остановить симуляцию
- **Step** - Выполнить один шаг симуляции
- **Reset** - Сбросить симуляцию

## Работа с файлами

### Сохранение схемы
1. Меню **File** → **Save**
2. Схема сохраняется в файл `circuit.lgsim`

### Загрузка схемы
1. Меню **File** → **Open**
2. Выберите файл `.lgsim`

## Горячие клавиши

- **Delete** - Удалить выбранный компонент (через меню Edit → Delete Selected)
- **Ctrl+N** - Новая схема (через меню File → New)
- **Ctrl+S** - Сохранить (через меню File → Save)

## Примеры схем

### Пример 1: Полусумматор

```
half adder
```

Создает схему с двумя входами (A, B) и двумя выходами (Sum, Carry).

### Пример 2: Простая логическая схема

```
add switch
add switch
add and
add led
```

Создает два переключателя, AND вентиль и светодиод.

### Пример 3: SR-защелка

```
sr latch
```

Создает SR-защелку с кнопками Set и Reset.

## Советы по использованию

1. **Используйте промпты для быстрого создания**: Вместо ручного размещения компонентов, используйте текстовые команды
2. **Тестируйте пошагово**: Используйте кнопку "Step" для отладки схем
3. **Сохраняйте часто**: Регулярно сохраняйте свои схемы
4. **Экспериментируйте**: Попробуйте разные комбинации компонентов

## Устранение неполадок

### Схема не работает
- Проверьте, что все компоненты правильно соединены
- Убедитесь, что симуляция запущена (кнопка Start или Step)
- Проверьте состояния входных переключателей

### Компонент не размещается
- Убедитесь, что вы выбрали компонент из палитры
- Кликните на свободное место на холсте

### Промпт не работает
- Проверьте синтаксис команды
- Введите `help` для просмотра доступных команд
- Введите `list` для просмотра всех компонентов

## Архитектура проекта

```
logisim/
├── main.py                 # Главный файл приложения
├── circuit_model.py        # Модель схемы (Circuit, Component, Wire)
├── simulation_engine.py    # Движок симуляции
├── file_handler.py         # Сохранение/загрузка файлов
├── components/             # Компоненты
│   ├── base.py            # Базовые классы
│   ├── gates.py           # Логические вентили
│   ├── io.py              # Ввод/вывод
│   ├── memory.py          # Элементы памяти
│   ├── complex.py         # Сложные компоненты
│   └── registry.py        # Реестр компонентов
├── ui/                     # Пользовательский интерфейс
│   ├── circuit_canvas.py  # Холст для рисования
│   ├── component_palette.py # Палитра компонентов
│   ├── simulation_controls.py # Управление симуляцией
│   └── prompt_interface.py # Интерфейс промптов
├── prompt_parser/          # Парсер промптов
│   ├── parser.py          # Основной парсер
│   └── templates.py       # Шаблоны схем
└── utils/                  # Утилиты
    ├── constants.py       # Константы
    └── geometry.py        # Геометрические функции
```

## Лицензия

Этот проект создан в образовательных целях.

## Контакты и поддержка

Для вопросов и предложений создайте issue в репозитории проекта.

---

**Приятного использования! 🚀**
